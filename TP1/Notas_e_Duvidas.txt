proc(){
if is primitive
	drawprimitive
else
	update(material, textura, transf)
	for(child in children):
		push(mat)
		push(txt)
		push(trans)
		proc(child)
		opop(mat)
		opop(txt)
		opop(trans)


para materais e texturas fazer classes proprias ou guarda-se s� em vetor a info toda

no display, para o no raiz dar uma matriz identidade como antecessor das transformações, uma material e uma textura default